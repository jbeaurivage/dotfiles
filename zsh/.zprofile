#Prompt variable (non oh-my-zsh)
#PROMPT="%m %c %# "

# User scripts
export PATH="$HOME/bin:$PATH"

# Cargo executables
export PATH="$PATH:$HOME/.cargo/bin"

# Docker containers
export PATH="$PATH:$HOME/docker-arm-dev"

# Enable xdg-portal for native file browser (e.g. firefox)
export GTK_USE_PORTAL=1
